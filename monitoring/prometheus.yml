global:
  scrape_interval: 15s

scrape_configs:
  - job_name: agri-yield-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:8000  # Docker Compose service name — works on all platforms
